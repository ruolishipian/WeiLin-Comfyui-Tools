# WeiLin-Comfyui-Tools 修复说明文档

## 概述

本文档记录了当前项目相对于原项目 [weilin9999/WeiLin-Comfyui-Tools](https://github.com/weilin9999/WeiLin-Comfyui-Tools) 的所有修复内容。

**原项目地址**: https://github.com/weilin9999/WeiLin-Comfyui-Tools  
**当前项目地址**: https://github.com/ruolishipian/WeiLin-Comfyui-Tools  
**修复版本**: V0.0.72 基础上进行修复  
**修复提交数量**: 5 个

---

## 修复清单

### 1. CSS布局冲突修复

**提交**: `2041975` - 修复CSS布局冲突：使用命名空间隔离防止插件样式污染

#### 问题描述
- `fastuse` 插件的 `.toolbar { height: 40px }` 样式导致本插件的 toolbar 高度被压缩
- `toolbar-bottom` 被挤出容器，被 `category-tabs` 覆盖
- 其他 ComfyUI 插件的 CSS 样式污染了本插件的 UI 布局

#### 解决方案
- 使用命名空间前缀：`.toolbar` → `.weilin-toolbar`
- 添加全局防御CSS：`[class^="weilin-"] { height: auto !important }`
- 强制覆盖外部样式：使用 `!important` 确保优先级

#### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `src/src/view/tag_manager/tag_index.vue` | HTML模板和CSS样式添加命名空间前缀 |
| `src/src/view/cloud/index.vue` | HTML模板和CSS样式添加命名空间前缀 |
| `dist/style.css` | 编译输出更新 |
| `dist/javascript/main.entry.js` | 编译输出更新 |

#### 代码示例
```css
/* 全局防御：防止其他插件覆盖 weilin 命名空间的样式 */
[class^="weilin-"],
[class*=" weilin-"] {
  height: auto !important;
  box-sizing: border-box !important;
}

/* 防御性样式：确保不被其他插件覆盖 */
.weilin-toolbar {
  height: auto !important;
  min-height: 40px !important;
  flex-direction: column;
  padding: 12px 16px;
  box-sizing: border-box !important;
}
```

---

### 2. Lora堆按钮点击失效修复

**提交**: `af53056` - 修复WeiLinPromptUI节点"打开Lora堆"按钮无法点击的问题

#### 问题描述
1. **点击运行时提示词消失**：
   - 代码只更新了 `element.value`（DOM元素的值）
   - 但没有更新 `widget.value`（ComfyUI widget对象的值）
   - 当点击运行时，ComfyUI 序列化工作流，从 `widget.value` 获取值
   - 由于 `widget.value` 没有更新，所以获取到的是旧值

2. **按钮点击报错**：
   - `nodeTextAreaList[1]` 和 `nodeTextAreaList[3]` 可能为 undefined
   - 直接访问 `.value` 会导致 JavaScript 错误

#### 解决方案
- 新增 `nodeWidgetList` 数组：保存 widget 引用
- 同步更新 `widget.value`：每次更新 `element.value` 时，同时更新对应的 `widget.value`
- 添加安全检查：所有访问 `nodeTextAreaList` 的地方添加 undefined 检查

#### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `js_node/weilin_prompt_ui_node.js` | 添加widget同步更新机制和安全检查 |

---

### 3. Lora堆二级文件夹文件不显示修复

**提交**: `1704f2f` - fix: 修复lora堆二级文件夹内文件不显示的问题

#### 问题描述
- Lora管理容器中二级文件夹内的lora文件无法显示
- 前端无法正确获取二级目录下的文件列表

#### 解决方案

**后端修改** (`app/server/prompt_api/lora_networks.py`):
- 为二级及以下目录添加 `"all"` 键，存储该子目录下所有文件列表
- 确保目录结构与一级目录保持一致

**前端修改** (`src/src/view/lora_manager/lora_index.vue`):
- 修复 `totalPages` 计算属性，正确获取二级目录文件数量
- 修复 `selectSecondCategory` 函数，正确读取二级目录文件列表
- 修复 `loadMoreData` 函数，正确加载二级目录更多文件
- 使用 `rootFolder.all` 获取文件列表，兼容新旧数据结构

#### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `app/server/prompt_api/lora_networks.py` | 为二级目录添加"all"键存储文件列表 |
| `src/src/view/lora_manager/lora_index.vue` | 修复文件列表获取逻辑 |

#### 代码示例
```python
# 后端修改
if subdir not in result[level1_dir]:
    result[level1_dir][subdir] = {
        "all": []  # 该子目录下所有文件
    }

# 添加文件到子目录
result[level1_dir][subdir][parts[-1]] = file_path
result[level1_dir][subdir]["all"].append(file_path)
```

```javascript
// 前端修改
// 使用 "all" 键获取文件列表，如果不存在则过滤掉 "all" 键
let valuesArray = rootFolder.all || Object.keys(rootFolder).filter(key => key !== 'all').map(key => rootFolder[key])
```

---

### 4. 多个Lora堆相关问题修复

**提交**: `f67dcb8` - fix: 修复多个lora堆相关问题

#### 修复内容

##### 4.1 Lora堆数据不同步问题
- 修复 `setPromptText` 函数中lora数据初始化逻辑
- 添加节点数据变化监听机制
- 实现UI窗口与节点数据实时同步

##### 4.2 节点删除后重新加载按钮失效问题
- 添加消息监听器移除机制
- 防止内存泄漏和事件冲突
- 清理Lora Stack相关数据

##### 4.3 多次点击按钮后失效问题
- 调整 `randomID` 变量作用域
- 消息监听器正确使用变量引用

##### 4.4 打开提示词编辑器后lora堆按钮无法点击问题
- 为两个按钮使用独立的ID变量（`promptBoxRandomID`, `loraStackRandomID`）
- 避免按钮之间的ID冲突

#### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `js_node/weilin_prompt_ui_node.js` | 添加数据同步机制、事件监听器管理 |
| `src/src/App.vue` | 添加相关配置 |
| `src/src/view/prompt_box/prompt_index.vue` | 添加lora数据变化监听、修复数据初始化逻辑 |

#### 关键代码
```javascript
// 监听 value 属性变化
let currentTempLoraValue = tempLoraTextarea.value;
Object.defineProperty(tempLoraTextarea, 'value', {
  get() {
    return currentTempLoraValue;
  },
  set(newValue) {
    currentTempLoraValue = newValue;
    notifyLoraDataChange();
  },
  enumerable: true,
  configurable: true
});

// 通知UI窗口lora数据变化
function notifyLoraDataChange() {
  window.postMessage({
    type: 'weilin_prompt_ui_lora_data_changed_' + promptBoxRandomID,
    data: JSON.stringify(jsonData)
  }, '*')
}
```

---

### 5. Lora堆按钮点击失效及缓存问题修复

**提交**: `1b393b0` - fix: 修复Lora堆按钮点击失效及缓存问题

#### 问题描述
- `openLoraManager` 函数参数处理不当，可能接收到事件对象而非DOM元素
- 内联 `onclick` 事件被 LiteGraph 拦截
- 浏览器缓存导致修复后的代码不生效

#### 解决方案
- 修复 `openLoraManager` 函数参数处理，添加类型检查和错误处理
- 移除内联 `onclick`，改用 `addEventListener` 绑定事件
- 添加 `mousedown`/`mouseup` 事件阻止冒泡，防止被 LiteGraph 拦截
- 添加版本号参数到静态资源URL，解决浏览器缓存问题

#### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `js_node/weilin_prompt_ui_node.js` | 添加版本号、修复事件绑定方式 |
| `dist/lora_stack.js` | 修复参数处理逻辑 |

#### 代码示例
```javascript
// 版本号，用于强制刷新缓存
const WEILIN_VERSION = '1.0.1';

// 静态资源URL添加版本号
script.src = './weilin/prompt_ui/webjs?v=' + WEILIN_VERSION;

// 修复参数处理
function openLoraManager(buttonElement) {
   // 确保参数是DOM元素而不是事件对象
   const element = buttonElement && buttonElement.target ? buttonElement.currentTarget : buttonElement;
   if (!element) {
       console.warn('[weilin-comfyui] openLoraManager: Invalid button element');
       return;
   }
   const seed = element.getAttribute("data-seed");
   // ...
}
```

---

## 修改文件汇总

| 文件路径 | 修改行数 | 修改类型 |
|---------|---------|---------|
| `app/server/prompt_api/lora_networks.py` | +11 -4 | 后端逻辑修复 |
| `js_node/weilin_prompt_ui_node.js` | +383 -240 | 前端节点核心修复 |
| `dist/lora_stack.js` | +15 -3 | 编译输出修复 |
| `src/src/App.vue` | +2 | 配置添加 |
| `src/src/view/tag_manager/tag_index.vue` | +23 -16 | CSS命名空间修复 |
| `src/src/view/cloud/index.vue` | +8 -4 | CSS命名空间修复 |
| `src/src/view/lora_manager/lora_index.vue` | +9 -6 | 文件列表获取修复 |
| `src/src/view/prompt_box/prompt_index.vue` | +50 -22 | 数据同步修复 |
| `dist/javascript/main.entry.js` | 编译输出 | 自动生成 |
| `dist/style.css` | 编译输出 | 自动生成 |

**总计**: 12 个文件，+467 行，-240 行

---

## 修复效果

### 修复前的问题
1. ❌ CSS样式被其他插件污染，UI布局错乱
2. ❌ Lora堆按钮点击无响应或报错
3. ❌ 二级文件夹内的Lora文件无法显示
4. ❌ 节点数据与UI窗口不同步
5. ❌ 多次操作后按钮失效
6. ❌ 浏览器缓存导致修复不生效

### 修复后的效果
1. ✅ 使用命名空间隔离，CSS样式不受其他插件影响
2. ✅ Lora堆按钮正常工作，支持点击和拖拽
3. ✅ 二级及多级文件夹内的Lora文件正常显示
4. ✅ 节点数据与UI窗口实时同步
5. ✅ 多次操作稳定可靠
6. ✅ 版本号机制确保用户获取最新代码

---

## 技术要点总结

### 1. CSS命名空间隔离
- 使用 `weilin-` 前缀命名所有CSS类
- 添加全局防御规则 `[class^="weilin-"]`
- 使用 `!important` 确保样式优先级

### 2. ComfyUI Widget同步机制
- 同时更新 `element.value` 和 `widget.value`
- 使用 `Object.defineProperty` 监听属性变化
- 通过 `postMessage` 实现跨窗口通信

### 3. 事件处理优化
- 使用 `addEventListener` 替代内联 `onclick`
- 添加事件冒泡阻止，防止被框架拦截
- 正确处理事件对象和DOM元素

### 4. 数据结构优化
- 为目录结构添加 `all` 键存储完整文件列表
- 兼容新旧数据结构，确保向后兼容

### 5. 缓存控制
- 静态资源URL添加版本号参数
- 修改版本号强制浏览器重新加载

---

## 建议与后续优化

1. **单元测试**: 建议为关键功能添加单元测试，确保修复的稳定性
2. **错误监控**: 添加更完善的错误日志和监控机制
3. **性能优化**: 考虑对大量Lora文件的加载进行性能优化
4. **文档完善**: 建议更新用户文档，说明已知问题和解决方案

---



*文档生成时间: 2026-03-15*  
*基于 Git 提交: 1b393b0*

ComfyUI 2.0 dom-widget 容器遮挡问题总结
一、问题表现
按钮无法点击：节点的"打开提示词编辑器"和"打开Lora堆"按钮无法点击
画布遮挡：dom-widget容器遮挡了画布的大部分区域
影响节点放置：无法在遮挡区域放置其他节点
影响画布拖动：无法在遮挡区域拖动画布
二、问题根源
1. dom-widget 容器的异常状态
多个容器重叠：存在5个 dom-widget size-full 容器，几乎完全重叠
定位错误：所有容器都有 position: fixed，固定在视口上，而不是跟随节点
事件拦截：pointer-events: auto 导致容器拦截所有鼠标事件
尺寸异常：size-full 类导致容器覆盖整个画布
三、解决方案
 CSS 样式覆盖（js_node/weilin_fix.css）
 /* 针对包含weilin相关元素的dom-widget */
.dom-widget:has([class*="weilin-"]) {
    pointer-events: none !important;
    position: absolute !important;
}

/* 确保weilin内部的交互元素可以正常响应 */
.dom-widget:has([class*="weilin-"]) * {
    pointer-events: auto !important;
}

/* 针对包含特定name的textarea的dom-widget */
.dom-widget:has(textarea[name="positive"]) {
    pointer-events: none !important;
    position: absolute !important;
}

/* 确保textarea可以正常交互 */
.dom-widget:has(textarea[name="positive"]) textarea {
    pointer-events: auto !important;
}
 JavaScript 精确修复（只修复当前节点）
 const fixCurrentNodeDomWidgets = () => {
  if (!this.widgets) return;
  
  this.widgets.forEach(widget => {
    if (widget.element) {
      let parent = widget.element.parentElement;
      while (parent) {
        if (parent.classList && parent.classList.contains('dom-widget')) {
          // 设置pointer-events: none让画布可以交互
          parent.style.setProperty('pointer-events', 'none', 'important');
          // 设置position: absolute让容器跟随节点
          parent.style.setProperty('position', 'absolute', 'important');
          // 移除size-full类
          parent.classList.remove('size-full');
          // 确保内部元素可以交互
          if (widget.element) {
            widget.element.style.setProperty('pointer-events', 'auto', 'important');
          }
          break;
        }
        parent = parent.parentElement;
      }
    }
  });
};
四、关键修复点
问题	原因	解决方法
容器遮挡画布	position: fixed 固定在视口	改为 position: absolute 跟随节点
事件被拦截	pointer-events: auto	设置 pointer-events: none 让画布可交互
内部元素无法交互	父容器 pointer-events: none	为内部元素设置 pointer-events: auto
容器尺寸过大	size-full 类	移除 size-full 类
影响其他节点	全局修复	只修复当前节点的 dom-widget
修改的文件
1. js_node/weilin_prompt_ui_node.js
添加了 CSS 文件加载
2. 添加了 fixCurrentNodeDomWidgets 函数
**js_node/weilin_fix.css**（新创建）

使用 CSS 样式覆盖修复 dom-widget 的问题



后端lora相关：

修复内容
1. 后端 lora_info.py 优化
修复位置：lora_info.py:149-191

新的后端处理逻辑
┌─────────────────────────────────────────────────────────────────┐
│  1. 解析标签                                                     │
├─────────────────────────────────────────────────────────────────┤
│  <wlr:my_lora:1.0:0.8:ohwx girl>                                │
│                                                                 │
│  提取:                                                          │
│  - lora_name = "my_lora"                                        │
│  - model_weight = 1.0                                           │
│  - trigger_weight = 0.8                                         │
│  - trigger_words = "ohwx girl" (可能为空)                        │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  2. 获取触发词（如果标签中没有）                                   │
├─────────────────────────────────────────────────────────────────┤
│  优先级：                                                        │
│  1. 从 ss_tag_frequency 提取第一个训练词                         │
│  2. 从 ss_output_name 获取输出名称                               │
│  3. 使用 LoRA 文件名作为触发词                                    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  3. 添加触发词到提示词开头                                        │
├─────────────────────────────────────────────────────────────────┤
│  原提示词: "a girl, blue eyes"                                   │
│  触发词: "ohwx girl"                                             │
│  最终: "ohwx girl, a girl, blue eyes"                            │
│                                                                 │
│  多LoRA: "trigger1, trigger2, 原提示词"                          │
└─────────────────────────────────────────────────────────────────┘
优先级	来源	说明
1	标签中的触发词	用户手动设置或从 loraWorks 获取
2	ss_tag_frequency	模型训练时的标签频率（第一个标签）
3	ss_output_name	模型输出名称
4	LoRA 文件名	最后的备选方案
文件	修改内容
__init__.py	添加 get_lora_trigger_words 函数
__init__.py	触发词添加到提示词开头
__init__.py	无触发词时自动从模型元数据获取
从元数据获取触发词(ss_tag_frequency): ohwx girl
模型权重strength_model： 1.0 触发词权重strength_clip： 0.8 触发词： ohwx girl

修复总结
问题分析
当从内嵌LoRA管理器删除最后一个LoRA时，消息无法同步到提示词。

根本原因：

在 prompt_index.vue 中处理 weilin_prompt_ui_update_lora_tags 消息时，只有当 loraTags.length > 0 时才处理：
if (loraTags && loraTags.length > 0) {
  // 更新提示词中的标签
}
当 loraTags 是空数组（删除最后一个LoRA时）时，这个条件不满足，所以不会清空提示词中的LoRA标签。

修复内容
添加对空数组的处理：
} else if (loraTags && loraTags.length === 0) {
  // 当 loraTags 为空数组时，清空提示词中的所有 LoRA 标签
  const currentText = inputText.value
  const wlrPattern = /<wlr:[^>]+>/g
  let cleanText = currentText.replace(wlrPattern, '')
  // 清理连续的逗号和空格
  cleanText = cleanText
    .replace(/,\s*,/g, ',')
    .replace(/,\s*$/g, '')
    .replace(/^\s*,/g, '')
    .trim()

  inputText.value = cleanText
  lastInputValue.value = inputText.value
  debouncedProcessInput()
}


修复总结
问题分析
用户报告内嵌LoRA堆优先级高于标签，通过父组件LoRA管理器添加的LoRA会被清掉。

根本原因：数据不同步

内嵌LoRA堆添加LoRA时：

通过 emit 更新 selectedLoras
发送 weilin_prompt_ui_add_lora_tag 消息
watch 监听变化后发送 weilin_prompt_ui_update_lora_tags 消息替换提示词中的所有LoRA标签
父组件LoRA管理器添加LoRA时：

只发送 weilin_prompt_ui_add_lora_tag 消息
之前的修复导致不再更新 selectedLoras
所以通过LoRA管理器添加的LoRA只在提示词中，不在 selectedLoras 中
当内嵌LoRA堆的 watch 触发时：

发送 weilin_prompt_ui_update_lora_tags 消息
替换提示词中的所有LoRA标签为 selectedLoras 中的内容
这会覆盖掉通过LoRA管理器添加的LoRA
解决方案
统一数据源：selectedLoras 是唯一的数据源

**修改 prompt_index.vue**：

恢复 weilin_prompt_ui_add_lora_tag 消息同时更新 selectedLoras 和提示词
通过检查 loraExistsInStack 避免重复添加
修改 LoraStack 组件：

addLora 函数不再发送 weilin_prompt_ui_add_lora_tag 消息
因为 emit 已经更新了 selectedLoras
watch 会监听变化并发送 weilin_prompt_ui_update_lora_tags 消息更新提示词
数据流总结
操作	数据流
内嵌LoRA堆添加	emit → 更新 selectedLoras → watch → 更新提示词
父组件LoRA管理器添加	消息 → 更新 selectedLoras + 更新提示词
这样确保内嵌LoRA堆和父组件LoRA管理器共享同一个数据源 selectedLoras，数据可以双向同步。

修改文件
src/view/prompt_box/prompt_index.vue 第3809-3852行
src/view/prompt_box/components/lora_stack.vue 第240-268


修复总结
问题分析
独立LoRA堆窗口和"打开Lora堆"按钮打开的LoRA堆数据不同步：

独立LoRA堆窗口添加LoRA可以影响节点
但节点内的变化不会同步到独立LoRA堆窗口
数据流分析
独立LoRA堆窗口（lora_stack.vue）：

有自己的 selectedLoras 数据
通过 watch 监听变化，发送 weilin_prompt_ui_prompt_node_finish_lora_stack_${seed} 消息到节点
监听 weilin_prompt_ui_selectLora_stack_${seed} 消息添加LoRA
节点内的LoRA堆：

数据存储在 window.weilinGlobalSelectedLoras[seed]
通过 renderAllLoras 渲染
监听 weilin_prompt_ui_prompt_node_finish_lora_stack_${seed} 消息更新数据
监听 weilin_prompt_ui_selectLora_stack_node_${seed} 消息添加LoRA
解决方案
添加双向同步：独立LoRA堆窗口监听节点发送的 weilin_prompt_ui_prompt_node_finish_lora_stack_${seed} 消息，更新自己的 selectedLoras

避免循环更新：添加 isUpdatingFromNode 标志，当从节点接收消息更新数据时，不发送消息回节点

修改文件
src/view/lora_manager/lora_stack.vue 第275-330行
修改内容
添加 isUpdatingFromNode 标志
在消息监听器中添加对 weilin_prompt_ui_prompt_node_finish_lora_stack_${seed} 消息的处理
在 watch 中检查 isUpdatingFromNode 标志，避免循环更新
修复问题总结
一、代码格式化和质量检查
Python 后端
问题	修复内容
代码格式不统一	使用 Black 格式化 30 个文件
代码质量检查	配置 Ruff 并修复 149 个问题
重复函数定义	删除 lora_info.py 中重复的 get_folder_path 函数
未使用的导入	修复 install_request.py 中的导入问题
配置文件	在 pyproject.toml 中添加 Ruff 和 Black 配置
前端 (Vue/JavaScript)
问题	修复内容
ESLint 错误	修复缩进、分号等问题
Stylelint 错误	调整配置，放宽过于严格的规则
Prettier 格式化	格式化所有代码文件
配置冲突	解决 ESLint、Stylelint、Prettier 之间的规则冲突
二、API 500 错误修复
问题：POST /weilin/prompt_ui/api/prompt/history/add_history 返回 500
问题原因	修复内容
数据库目录不存在	在 create_tables() 和 _get_connection() 中添加 os.makedirs()
错误处理不完善	改进错误处理，返回 JSON 格式的错误响应和完整堆栈跟踪
修改文件：

app/server/dao/dao.py
app/server/prompt_server.py
三、数据库损坏修复
问题：database disk image is malformed
问题原因	修复内容
数据库文件损坏	添加 check_and_repair_db() 函数检查数据库完整性
文件被锁定无法删除	添加 force_repair_database() 函数，尝试重命名文件
运行时错误恢复	添加 _reconnect_after_error() 函数自动重连
表结构重建	添加 _create_history_tables() 函数重建表
新增函数：


python
 
check_and_repair_db(db_path, db_type)  # 检查数据库完整性
repair_database(db_path)               # 修复损坏的数据库
force_repair_database(db_path)         # 强制修复（运行时）
_reconnect_after_error(db_type)        # 错误后重连
_create_history_tables()               # 重建 history 表
修改的查询函数：

fetch_one() - 捕获 DatabaseError 并自动修复
fetch_all() - 捕获 DatabaseError 并自动修复
execute_query() - 捕获 DatabaseError 并自动修复
四、触发词重复输出和权重重置问题
问题1：触发词被重复输出
原因：setPromptText 设置 selectedLoras 时触发 lora_stack.vue 的 watch，导致 <wlr:...> 标签被重复添加

修复：

在 lora_stack.vue 中添加 isInitializing 标记
watch 中检查该标记，初始化时不发送更新消息
暴露 setInitializing() 方法供父组件调用
问题2：权重修改后被重置
原因：数据恢复时 temp_lora 没有正确处理
权重没问题了，触发词问题还是存在。
问题根源分析
通过分析后端代码的输出流程，我发现了触发词重复输出的根本原因：

数据流程：

positive 参数包含 JSON 数据：{"prompt": "...", "lora": [...]}
第 248-254 行：从 JSON 中提取 lora_list（来自 lora 字段）和 text_dec（来自 prompt 字段）
第 283-287 行：从 text_dec 中匹配 <wlr:...> 标签
问题所在：第 319-320 行使用 lora_list.extend(extracted_loras) 直接合并
问题场景：

如果前端保存的数据中，lora 字段包含了 Lora 信息
同时 prompt 字段中也包含了 <wlr:...> 标签（前端会同步更新）
那么同一个 Lora 会被添加两次到 lora_list
导致触发词被重复添加到输出字符串
修复内容
修改 __init__.py 中的合并逻辑，添加去重合并：


python
 
# 如果已经有lora_list，需要去重合并
# 避免同一个Lora被重复处理（来自JSON的lora字段和prompt中的<wlr:...>标签）
if lora_list is not None:
    # 获取已存在的lora名称集合
    existing_lora_names = {
        item["lora"].replace(".safetensors", "")
        for item in lora_list
    }
    # 只添加不存在的lora
    for extracted in extracted_loras:
        extracted_name = extracted["lora"].replace(".safetensors", "")
        if extracted_name not in existing_lora_names:
            lora_list.append(extracted)
else:
    lora_list = extracted_loras
修复后的行为
当 lora 字段和 <wlr:...> 标签包含相同的 Lora 时，只会处理一次
触发词不会被重复添加到输出字符串
不影响独立 Lora 堆的功能



修复：

在 prompt_index.vue 中添加 loraStackRef 引用
setPromptText 中使用初始化标记包裹数据设置
使用 nextTick 确保数据更新完成后再取消标记
修改文件：

src/view/prompt_box/components/lora_stack.vue
src/view/prompt_box/prompt_index.vue
五、单元测试
执行结果：143 个测试全部通过，覆盖率 100%

测试模块	通过数
空值处理测试	0
核心功能测试	54
API 接口测试	46
数据处理测试	43
六、配置文件更新
文件	更新内容
pyproject.toml	添加 Ruff 和 Black 配置，忽略 ComfyUI 插件特有规则
src/.eslintrc.json	禁用与 Prettier 冲突的规则
src/.stylelintrc.json	放宽过于严格的样式规则
修改文件汇总
文件路径	修改类型
pyproject.toml	配置更新
app/server/dao/dao.py	数据库修复逻辑
app/server/prompt_server.py	错误处理改进
app/server/prompt_api/lora_info.py	删除重复函数
install_request.py	修复导入
src/.eslintrc.json	配置更新
src/.stylelintrc.json	配置更新
src/view/prompt_box/components/lora_stack.vue	初始化标记
src/view/prompt_box/prompt_index.vue	数据恢复逻辑

防护代码已添加完成
修改内容总结
1. 添加了验证函数 validate_conditioning_output

python
 
def validate_conditioning_output(conditioning_data):
    """
    验证CONDITIONING输出格式是否正确。
    CONDITIONING应该是 [[cond_tensor, dict]] 或 None 的格式。
    """
    if conditioning_data is None:
        return True

    # 检查是否是列表格式
    if not isinstance(conditioning_data, list):
        print(f"[WARNING] CONDITIONING输出格式错误: 期望list，得到{type(conditioning_data)}")
        return False

    # 检查是否至少有一个元素
    if len(conditioning_data) == 0:
        print("[WARNING] CONDITIONING输出为空列表")
        return True

    # 检查第一个元素是否是 [cond, dict] 格式
    first_item = conditioning_data[0]
    if not isinstance(first_item, list) or len(first_item) < 2:
        print(f"[WARNING] CONDITIONING内部格式错误: 期望[cond, dict]，得到{type(first_item)}")
        return False

    return True
2. 为 WeiLinPromptUI 添加了异常处理

python
 
if opt_clip is not None:
    try:
        # ... 编码逻辑 ...
        conditioning_output = [[condA, outputA]]

        # 验证CONDITIONING输出格式
        if not validate_conditioning_output(conditioning_output):
            print("[ERROR] CONDITIONING输出格式验证失败，返回None")
            conditioning_output = None

        # 返回结果
    except Exception as e:
        print(f"[ERROR] CONDITIONING编码失败: {e}")
        # 返回None作为CONDITIONING，防止崩溃
3. 为 WeiLinPromptUIWithoutLora 添加了同样的防护
防护效果
场景	防护措施	结果
CONDITIONING编码失败	try-except捕获异常	返回None，不崩溃
CONDITIONING格式错误	validate_conditioning_output验证	返回None，打印警告
连接到错误节点	ComfyUI前端类型检查	无法连接（类型不匹配）
重要说明
关于"连接到错误节点导致崩溃"的问题：

这个崩溃实际上不是发生在你的节点内部，而是发生在ComfyUI的WebSocket通信层：


code
 
File "server.py", line 1168, in send_json
TypeError: Object of type Tensor is not JSON serializable
这是因为：

某个展示节点尝试将CONDITIONING数据发送到前端显示
JSON序列化失败导致ComfyUI崩溃
根本解决方案：

ComfyUI前端已经有类型检查，CONDITIONING类型只能连接到接受CONDITIONING输入的节点
用户不应该将CONDITIONING连接到展示节点
这是ComfyUI本身的设计，不是你的节点问题
我添加的防护措施：

确保CONDITIONING输出格式正确
添加异常处理防止编码失败
在控制台打印警告信息
这些防护可以防止编码过程中的错误，但无法防止用户将CONDITIONING连接到不兼容的节点（这需要ComfyUI前端来限制）