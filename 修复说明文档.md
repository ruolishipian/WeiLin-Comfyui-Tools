# WeiLin-Comfyui-Tools 修复说明文档

## 概述

本文档记录了当前项目相对于原项目 `H:\ai\WeiLin-Comfyui-Tools-main原项目\WeiLin-Comfyui-Tools-main` 的所有修复和改进内容。

**原项目路径**: `H:\ai\WeiLin-Comfyui-Tools-main原项目\WeiLin-Comfyui-Tools-main`
**当前项目路径**: `H:\ai\weilin-comfyui-tools`
**修复日期**: 2026-03-19

---

## 一、项目结构变化

### 1.1 新增文件和目录

| 文件/目录 | 说明 |
|----------|------|
| `.arts/` | CodeArts IDE 配置目录 |
| `.codeartsdoer/` | CodeArts Doer 配置目录 |
| `.git/` | Git 版本控制目录 |
| `.ruff_cache/` | Ruff Python 代码检查缓存 |
| `init.json` | 初始化配置文件 |
| `tag_labels.json` | 标签标签配置文件 |
| `tests/` | 测试代码目录 |
| `user_cloud_package.json` | 用户云包配置 |
| `yarn.lock` | Yarn 包管理器锁定文件 |
| `js_node/weilin_fix.css` | ComfyUI 2.0 兼容性修复 CSS |
| `src/` | 前端源码重构目录（Vue 3 项目） |
| `PERFORMANCE_OPTIMIZATION_PLAN.md` | 性能优化计划文档 |

### 1.2 前端项目重构

原项目的 `src/` 目录结构简单，当前项目进行了完整的前端重构：

**原项目 src/ 目录**:
```
src/
├── .gitignore
├── index.html
├── package.json
├── package-lock.json
├── vite.config.js
└── src/ (子目录)
```

**当前项目 src/ 目录**:
```
src/
├── .eslintignore
├── .eslintrc.json
├── .gitignore
├── .husky/
├── .lintstagedrc.json
├── .npmrc
├── .prettierignore
├── .prettierrc.json
├── .stylelintignore
├── .stylelintrc.json
├── .yarnrc.yml
├── api/
├── App.vue
├── check-dependencies.js
├── components/
├── i18n/
├── index.html
├── main.js
├── node_modules/
├── package.json
├── stores/
├── style.css
├── styles/
├── utils/
├── view/
├── vite.config.js
├── yarn.lock
└── YARN_GUIDE.md
```

**改进内容**:
- 采用 Vue 3 + Vite 架构
- 添加 ESLint、Prettier、Stylelint 代码规范工具
- 添加 Husky + lint-staged Git 提交钩子
- 使用 Yarn 2 (Yarn Berry) 包管理器
- 模块化组件设计（api、components、stores、utils、view）
- 国际化支持（i18n 目录）

---

## 二、核心代码修复

### 2.1 `__init__.py` - ComfyUI 2.0 兼容性修复

#### 修复内容

**1. 添加 ComfyUI 2.0 JavaScript 目录注册**

```python
# 新增代码（当前项目第 1-9 行）
import os
import nodes

# ComfyUI 2.0兼容性：使用nodes.EXTENSION_WEB_DIRS注册JavaScript目录
# 必须在其他导入之前设置
custom_node_dir = os.path.dirname(os.path.realpath(__file__))
js_dir = os.path.join(custom_node_dir, "js_node")
nodes.EXTENSION_WEB_DIRS["weilin-comfyui-tools"] = js_dir
```

**说明**: ComfyUI 2.0 改变了 JavaScript 扩展目录的注册方式，原项目使用 `WEB_DIRECTORY = "./js_node"` 的方式已不兼容，需要通过 `nodes.EXTENSION_WEB_DIRS` 注册。

**2. 添加 CONDITIONING 输出格式验证函数**

```python
def validate_conditioning_output(conditioning_data):
    """
    验证CONDITIONING输出格式是否正确。
    CONDITIONING应该是 [[cond_tensor, dict]] 或 None 的格式。
    这个函数用于确保输出数据结构正确，防止连接到错误节点时引发问题。
    """
    if conditioning_data is None:
        return True

    # 检查是否是列表格式
    if not isinstance(conditioning_data, list):
        print(f"[WARNING] CONDITIONING输出格式错误: 期望list，得到{type(conditioning_data)}")
        return False

    # 检查是否至少有一个元素
    if len(conditioning_data) == 0:
        print("[WARNING] CONDITIONING输出为空列表")
        return True

    # 检查第一个元素是否是 [cond, dict] 格式
    first_item = conditioning_data[0]
    if not isinstance(first_item, list) or len(first_item) < 2:
        print(f"[WARNING] CONDITIONING内部格式错误: 期望[cond, dict]，得到{type(first_item)}")
        return False

    return True
```

**说明**: 防止 CONDITIONING 输出格式错误导致连接到其他节点时崩溃。

**3. 添加 LoRA 触发词自动提取功能**

```python
def get_lora_trigger_words(lora_path, lora_name):
    """
    从LoRA文件中获取触发词
    优先级：
    1. 从模型元数据中提取第一个训练词 (ss_tag_frequency)
    2. 从模型元数据中获取输出名称 (ss_output_name)
    3. 使用LoRA文件名作为触发词
    """
    trigger_words = ""

    try:
        # 尝试从safetensors文件头读取元数据
        if lora_path.endswith(".safetensors"):
            with open(lora_path, "rb") as file:
                # 读取header大小
                header_size = int.from_bytes(file.read(8), "little", signed=False)
                if header_size > 0:
                    header = file.read(header_size)
                    header_json = json.loads(header)
                    metadata = header_json.get("__metadata__", {})

                    # 优先级1: 从ss_tag_frequency提取第一个训练词
                    if "ss_tag_frequency" in metadata:
                        tag_freq = metadata["ss_tag_frequency"]
                        if isinstance(tag_freq, dict):
                            for bucket_value in tag_freq.values():
                                if isinstance(bucket_value, dict):
                                    for tag, count in bucket_value.items():
                                        if tag and tag.strip():
                                            trigger_words = tag.strip()
                                            return trigger_words

                    # 优先级2: 从ss_output_name获取
                    if "ss_output_name" in metadata:
                        output_name = metadata["ss_output_name"]
                        if output_name and output_name.strip():
                            trigger_words = output_name.strip()
                            # 去除常见的版本号后缀
                            trigger_words = re.sub(r"[-_]v?[0-9]+$", "", trigger_words)
                            return trigger_words

    except Exception as e:
        print(f"读取LoRA元数据失败: {e}")

    # 优先级3: 使用LoRA文件名作为触发词
    if lora_name and lora_name.strip():
        trigger_words = lora_name.strip()
        trigger_words = re.sub(r"[-_]v?[0-9]+$", "", trigger_words)

    return trigger_words
```

**说明**: 自动从 LoRA 文件元数据中提取触发词，并添加到提示词开头，提高生成质量。

**4. 支持新的 `<wlr:...>` 标签格式**

原项目只支持 3 参数格式：
```
<wlr:lora_name:model_weight:clip_weight>
```

当前项目支持 4 参数格式（新增触发词权重）：
```
<wlr:lora_name:model_weight:clip_weight:trigger_weight>
```

**代码改进**:
```python
# 匹配两种格式：
# 旧格式(3参数): <wlr:name:model_weight:clip_weight>
# 新格式(4参数): <wlr:name:model_weight:clip_weight:trigger_weight>
wlr_pattern_new = r"<wlr:([^:]+):([^:]+):([^:]+):([^>]+)>"
wlr_pattern_old = r"<wlr:([^:]+):([^:]+):([^:>]+)>"

wlr_matches_new = re.findall(wlr_pattern_new, text_dec)

# 从文本中移除新格式标签，再匹配旧格式，避免重复匹配
text_dec_without_new = re.sub(wlr_pattern_new, "", text_dec)
wlr_matches_old = re.findall(wlr_pattern_old, text_dec_without_new)
```

**5. 添加 LoRA 去重逻辑**

```python
# 如果已经有lora_list，需要去重合并
# 避免同一个Lora被重复处理（来自JSON的lora字段和prompt中的<wlr:...>标签）
if lora_list is not None:
    # 获取已存在的lora名称集合
    existing_lora_names = {
        item["lora"].replace(".safetensors", "") for item in lora_list
    }
    # 只添加不存在的lora
    for extracted in extracted_loras:
        extracted_name = extracted["lora"].replace(".safetensors", "")
        if extracted_name not in existing_lora_names:
            lora_list.append(extracted)
else:
    lora_list = extracted_loras
```

**6. 添加异常处理**

在 `WeiLinPromptUI.load_lora_ing()` 和 `WeiLinPromptUIWithoutLora.encode()` 方法中添加了 try-except 异常处理，防止 CONDITIONING 编码失败导致整个节点崩溃。

```python
if opt_clip is not None:
    try:
        tokensA = clip_lora_secondA.tokenize(text_dec)
        outputA = clip_lora_secondA.encode_from_tokens(
            tokensA, return_pooled=True, return_dict=True
        )
        condA = outputA.pop("cond")
        conditioning_output = [[condA, outputA]]

        # 验证CONDITIONING输出格式
        if not validate_conditioning_output(conditioning_output):
            print("[ERROR] CONDITIONING输出格式验证失败，返回None")
            conditioning_output = None

        # ... 返回结果
    except Exception as e:
        print(f"[ERROR] CONDITIONING编码失败: {e}")
        # 返回 None 作为 CONDITIONING
```

**7. 代码风格改进**

- 使用 `is not None` 替代 `!= None`
- 使用 `if auto_random:` 替代 `if auto_random == True:`
- 添加类型提示和文档字符串
- 代码格式化（符合 PEP 8 规范）

---

### 2.2 `js_node/weilin_fix.css` - ComfyUI 2.0 UI 兼容性修复

**新增文件**: `js_node/weilin_fix.css`

**问题分析**:
1. 存在多个 `dom-widget size-full` 容器，几乎重叠
2. 所有容器都有 `position: fixed`，固定在视口上
3. `pointer-events: auto` 导致事件被拦截
4. 输入框（textarea）被隐藏（`display: none`）

**解决方案**:
```css
/* 方案1：针对包含weilin相关元素的dom-widget */
.dom-widget:has([class*="weilin-"]) {
    pointer-events: none !important;
    position: absolute !important;
}

/* 确保weilin内部的交互元素可以正常响应 */
.dom-widget:has([class*="weilin-"]) * {
    pointer-events: auto !important;
}

/* 方案2：针对包含特定name的textarea的dom-widget */
.dom-widget:has(textarea[name="positive"]) {
    pointer-events: none !important;
    position: absolute !important;
}

/* 确保textarea可以正常交互 */
.dom-widget:has(textarea[name="positive"]) textarea {
    pointer-events: auto !important;
}

/* 方案3：针对size-full类的通用修复 */
.dom-widget.size-full:not(:has(button)):not(:has(input[type="checkbox"])):not(:has(select)) {
    pointer-events: none !important;
}

/* 确保size-full内部的交互元素可以正常响应 */
.dom-widget.size-full textarea,
.dom-widget.size-full input,
.dom-widget.size-full select,
.dom-widget.size-full button {
    pointer-events: auto !important;
}
```

**说明**: 解决 ComfyUI 2.0 中 dom-widget 容器遮挡画布的问题，确保用户可以正常交互。

---

### 2.3 `app/server/prompt_api/lora_networks.py` - Lora 二级目录修复

**问题**: Lora 管理容器中二级文件夹内的 lora 文件无法显示。

**修复代码** (第 172-179 行):

```python
# 确保子目录在一级目录下，并包含"all"键
if subdir not in result[level1_dir]:
    result[level1_dir][subdir] = {"all": []}  # 该子目录下所有文件

# 添加文件到子目录，使用完整路径
result[level1_dir][subdir][parts[-1]] = file_path
result[level1_dir][subdir]["all"].append(file_path)  # 新增：添加到子目录的all列表
result[level1_dir]["all"].append(file_path)
```

**原项目代码**:
```python
# 确保子目录在一级目录下
if subdir not in result[level1_dir]:
    result[level1_dir][subdir] = {}

# 添加文件到子目录，使用完整路径
result[level1_dir][subdir][parts[-1]] = file_path
result[level1_dir]["all"].append(file_path)
```

**说明**: 为二级及以下目录添加 `"all"` 键，存储该子目录下所有文件列表，确保前端可以正确获取二级目录下的文件。

---

### 2.4 `install_request.py` - 代码风格改进

**改进内容**:
- 代码格式化（符合 PEP 8 规范）
- 使用 `__import__("uuid_extensions")` 替代 `from uuid_extensions import uuid7`（避免未使用警告）

---

## 三、其他文件修改

### 3.1 配置文件修改

| 文件 | 修改内容 |
|------|---------|
| `.gitattributes` | Git LFS 配置调整 |
| `.gitignore` | 忽略规则更新 |
| `pyproject.toml` | 项目配置更新 |
| `requirements.txt` | 依赖项保持不变（uuid7, aiosqlite） |
| `README.md` | 文档更新 |
| `README_EN.md` | 英文文档更新 |
| `LICENSE` | 许可证文件更新 |

### 3.2 编译输出文件修改

| 文件 | 说明 |
|------|------|
| `dist/javascript/main.entry.js` | 前端编译输出 |
| `dist/javascript/main.entry.js.map` | Source Map |
| `dist/lora_stack.css` | Lora 堆样式 |
| `dist/lora_stack.js` | Lora 堆脚本 |
| `dist/style.css` | 主样式文件 |
| `dist/weilin-comfyui-tools.css` | 新增样式文件 |

### 3.3 后端服务文件修改

以下文件均有代码风格改进和格式化：

- `app/server/ai_server/siliconflow.py`
- `app/server/ai_translator/ai_translator.py`
- `app/server/cloud_warehouse/save_history.py`
- `app/server/cloud_warehouse/warehouse.py`
- `app/server/dao/dao.py`
- `app/server/fast_autocomplete/autocomplete.py`
- `app/server/history/collect_history_contl.py`
- `app/server/history/history_contl.py`
- `app/server/launch.py`
- `app/server/panel_server/server_init.py`
- `app/server/panel_server/tag_manager.py`
- `app/server/panel_server/tag_manager_handel.py`
- `app/server/panel_server/ymal_utils.py`
- `app/server/prompt_api/danbooru.py`
- `app/server/prompt_api/lora_info.py`
- `app/server/prompt_api/random_tag_template.py`
- `app/server/prompt_api/tags_manager.py`
- `app/server/prompt_server.py`
- `app/server/translate/api_translate.py`
- `app/server/translate/local_translate.py`
- `app/server/translate/network_translate/alibabav2.py`
- `app/server/translate/network_translate/baidu.py`
- `app/server/translate/network_translate/bing.py`
- `app/server/translate/network_translate/wangyi.py`
- `app/server/translate/new_translate_api.py`
- `app/server/translate/openai_translate.py`
- `app/server/user_init/user_init.py`

**主要改进**:
- 代码格式化（符合 PEP 8 规范）
- 使用 f-string 替代字符串拼接
- 添加类型提示
- 改进异常处理
- 优化导入顺序

---

## 四、总结

### 4.1 主要修复内容

1. **ComfyUI 2.0 兼容性修复**
   - JavaScript 目录注册方式更新
   - UI 布局兼容性修复（weilin_fix.css）

2. **功能增强**
   - LoRA 触发词自动提取
   - 支持新的 `<wlr:...>` 标签格式（4 参数）
   - LoRA 去重逻辑

3. **Bug 修复**
   - Lora 二级目录文件不显示
   - CONDITIONING 输出格式验证
   - 异常处理改进

4. **代码质量提升**
   - 前端项目重构（Vue 3 + Vite）
   - 代码风格统一（PEP 8）
   - 添加代码规范工具（ESLint、Prettier、Stylelint）
   - 添加 Git 提交钩子（Husky + lint-staged）

### 4.2 兼容性说明

- **ComfyUI 版本**: 兼容 ComfyUI 2.0
- **Python 版本**: Python 3.8+
- **Node.js 版本**: Node.js 16+（前端构建）

### 4.3 升级建议

1. 如果从原项目升级，建议备份现有配置
2. 删除旧的 `__pycache__` 目录
3. 重新安装依赖：`pip install -r requirements.txt`
4. 前端开发需要安装依赖：`cd src && yarn install`

---

**文档生成日期**: 2026-03-19
**文档版本**: 1.0
